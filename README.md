# portfolio-v2

Shaurya Gulati — portfolio, second edition.

Vite + React Router (static prerender) + React Three Fiber. Terminal with a built-in AI agent over my work.

```bash
npm install
npm run dev      # local dev
npm run build    # static site -> build/client
```
